// A string is a sequence of letters, numbers, special characters and arithmetic values or combination of all.

let str1 = "Trupti";

console.log(str1.length);//length returns the length of string