//length is a property in javascript which returns the length of specified array

let fruits = ["mango" , "papaya" , "apple" , "pineapple" , "orange"];

console.log("length : " + fruits.length);

//accessing through forEach

fruits.forEach(element => {
    console.log(element);
});